/*
 think of something you could store in a TypeScript Object.
Write a program that creates Objects containing these items.
*/
var book = {
    title: "the picnic",
    author: "taha qureshi",
    publishYear: 2024
};
console.log("book info:".concat(book.title, " by ").concat(book.author, ", publish year:").concat(book.publishYear));
